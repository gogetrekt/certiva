# ──────────────────────────────────────────────────────────────────────────────
# Certiva Web (Next.js) — Environment Configuration Example
# Copy to apps/web/.env and fill in real values.
# NEVER commit apps/web/.env to source control.
#
# This file contains only Next.js / BFF (route handler) config.
# No JWT_SECRET, DATABASE_URL, REDIS_URL, or PRIVATE_KEY belong here.
# ──────────────────────────────────────────────────────────────────────────────

# ── Runtime mode ──────────────────────────────────────────────────────────────
# NODE_ENV: Next.js build and runtime mode
# APP_ENV:  deployment context (development / staging / production)
NODE_ENV=production
APP_ENV=staging

# ── Internal API URL ──────────────────────────────────────────────────────────
# Server-side only. Used by Next.js Route Handlers to call the NestJS API.
# This URL is NEVER sent to the browser.
# On same machine: http://127.0.0.1:4000/api
# Separate server: http://<internal-api-host>:4000/api
INTERNAL_API_URL=http://127.0.0.1:4000/api

# ── Cookie security ───────────────────────────────────────────────────────────
# true when browser accesses the app via HTTPS (Cloudflare, reverse proxy).
# Must match the COOKIE_SECURE value used by the API process.
COOKIE_SECURE=true

# ── Public browser-safe variables (NEXT_PUBLIC_ prefix) ───────────────────────
# These are inlined into the browser bundle at build time.
# Only put values here that are safe to expose publicly.

# Public-facing site URL (used for canonical links, QR codes, metadata).
NEXT_PUBLIC_SITE_URL=https://your-domain.example

# Support contact email displayed on the contact page.
NEXT_PUBLIC_SUPPORT_EMAIL=support@your-domain.example
